package com.techelevator.services;

public class FirstLoginService {
    //For handling the password change after login with the temporary password
    //Validate new password, hash, update volunteer, disable tempPasswordActive, set firstLogin to false.
}
